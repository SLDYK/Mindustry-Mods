# RA2 操作规范（ra2-controls 实现基准 · 仅保留直接可行操作）

> **用途**：本模组实现的操作规范。源自《红色警戒2》原版默认操作，已裁剪为 Mindustry 指挥模式下**可直接实现**的条目；不可实现的 RA2 特性集中列在 §7（不实现），实现时以本文为准。
> 键位为模组默认值，全部可在设置中改绑。与 `PLAN.md` §3 的实现方案表一一对应（2026-09-04 裁剪）。

---

## 0. 鼠标方案（唯一方案，鼠标键位固定不可改）

| 操作 | 键 |
|------|-----|
| 选择（单位/建筑/框选） | **左键** |
| 下令（移动/攻击） | **左键**（有单位被选中时） |
| 清空选择 | **右键单击**（右键拖拽无操作） |
| 队列指令 | **Shift + 左键**（命令依次排队执行） |
| 路径点模式 | **Z 按住 + 左键连点**（RA2 原版键位）`[增强]` |

记忆口诀："左键打天下，右键收摊"。

> Ctrl 键在 Mindustry 侧的语义改为：Ctrl 框选剔除；不再承担 RA2 的强制开火（协议不支持，见 §7）。路径点连点由 **Z** 承担。
> 注：指挥模式切换键由模组改绑为 **Tab**（原版默认左 Ctrl，与框选剔除打架；Shift 留给加选）。仅在首次启用时一次性迁移，之后在设置里手动改绑的值模组不覆盖；Tab 原版默认绑 playerList（联机玩家列表，单机无影响）。
> 指挥模式**进入方式强制为切换**（模组托管原版「按住」设置 commandmodehold；RA2 原版无按住式进入）。
>
> **镜头：完全保留 Mindustry 原版机制（已定，模组零改动）**——镜头每帧跟随玩家操控的单位（操控无人机时即跟随无人机）；平移手段为 WASD（移动单位随之跟随）、鼠标前进侧键、小地图点击，均为原版行为。RA2 的"右键拖拽平移镜头"与"屏幕边缘滚动"不实现（见 §7）。

---

## 1. 总原则

1. **左键 = 选择 + 下令，右键 = 清空选择**。
2. **光标即语义**：悬停不同对象时光标实时显示"点下去会执行什么命令"。
3. **零面板操作**：常用指令 ≤ 1 次按键 + 1 次点击。
4. 键盘管**状态与编队**（攻击移动/停止/散开/编队），鼠标管**选择与下令**。

---

## 2. 选择逻辑

| 操作 | 行为 |
|------|------|
| 左键单击己方单位 | 选中它，**替换**当前选择 |
| 左键单击己方建筑 | 选中建筑（战斗建筑选中后可下令攻击） |
| 左键单击敌方单位 | 无效 |
| 左键按住拖拽 | **框选**：松开时选中框内所有**单位**（不含建筑），替换原选择 |
| Shift + 左键单击 | 该单位加入/移出选择（toggle） |
| Shift + 框选 | 框内单位**并入**当前选择 |
| Ctrl + 框选 | 框内单位从选择中**剔除**（可选，默认关）`[增强]` |
| 双击单位 | 选中**屏幕内**所有同类型单位 |
| 三击单位 | 选中**世界内**所有同类型单位（可选）`[增强]` |
| 右键单击 | 清空全部选择 |

**无选择时左键单击空地** = 无操作；**有选择时** = 移动（见 §3）。

---

## 3. 下令逻辑

有单位被选中时，**左键**点击目标，命令按目标类型解析：

| 左键目标 | 命令 | 说明 |
|----------|------|------|
| 敌方单位/建筑 | **攻击** | 队伍集火该目标 |
| 空地 | **移动** | 队伍移动到该点 |

要点：
- 移动是**松散阵型**：沿用 Mindustry 原生阵型分配（同批次 + 同目标点 + 同物理层）。
- 行进中的单位会**边走边打**射程内的敌人，但**不停步、不追击**（RA2 语义；Mindustry 单位默认行为天然一致，无需额外实现）。
- 战斗建筑选中后，左键点敌人 = 指定其攻击目标。
- **Shift + 左键** = 将命令**排队**（依次执行）。

---

## 4. 光标状态机

| 悬停对象 | 光标形态 | 左键点击后命令 |
|----------|---------|-----------|
| 空地（有选择时） | 绿色移动箭头 | 移动 |
| 敌方单位/建筑 | 红色攻击准星 | 攻击 |
| 攻击移动待命（按 G 后） | 红色十字 | 攻击移动 |
| 未选中任何单位 | 普通箭头 | 选择 |

> 右键光标始终是普通箭头（清空选择），不随悬停对象变化。

---

## 5. 快捷键总表

> 以下键位为**默认值**，全部注册为模组自定义 KeyBind，在 **设置→控制→RA2 指令** 分组中可自行改绑/重置（指挥模式切换键为原版键位，同样可改绑）。

### 镜头

| 键 | 功能 |
|----|------|
| WASD | 移动玩家单位，镜头随之跟随（原版机制） |
| 鼠标前进侧键 | 按住平移镜头（原版已有） |
| 小地图点击 | 跳转镜头（原版已有） |
| Home | 一次性跳回最近核心视角（模组新增，走原版 `panCamera`，不改变跟随机制） |
| 其余（缩放/暂停等） | 沿用 Mindustry 原生键位，不改动 |

> 镜头跟随/平移机制 100% 保留原版，模组不做任何镜头逻辑改动；RA2 的右键拖镜头与屏幕边缘滚动不实现（见 §7）。

### 单位指令

| 键 | 功能 | 说明 |
|----|------|------|
| G | 攻击移动（待命） | 按后左键点地：沿路自动接敌，到点结束（Tier A 近似 / Tier B 交战后继续前往目标点） |
| F | 停止 | 清空全部指令，原地待命（仍自动还击射程内敌人）；即 RA2 的 S，因 WASD 占用先移到 H，实机发现原版 H 被"选中全部单位工厂"占用，改用 F（其原版绑定仅蓝图模式生效，无冲突） |
| X | 散开 | 各单位向自身周围就近散点下令 |
| T | 选择同类 | 选中屏幕内所有同类型单位（与双击等价，可选） |

### 编队（沿用 Mindustry 原生逻辑）

| 键 | 功能 |
|----|------|
| Ctrl + 0–9 | 把当前选择存为该编号编队（覆盖同号） |
| 0–9 | 选中该编队 |
| 双击数字键 | 选中并**居中镜头**到该编队 |

---

## 6. 路径点

- **Z 按住 + 左键连点**（RA2 原版键位）：逐点生成编号节点，所选单位按顺序依次经过，松开 Z 时按序批量下发（排入队列）。
- 行进中本地绘制编号旗帜（RA2 原版路径点不可视化）`[增强]`。

---

## 7. 不实现的 RA2 特性（集中记录，避免重复评估）

| RA2 特性 | 不做的原因 |
|----------|-----------|
| Ctrl + 左键 强制开火 | Mindustry 下令协议无 force-fire 字段（PLAN §1.2 out of scope） |
| Alt + 左键 强制移动/碾压 | 寻路与碾压模型完全不同 |
| 进驻建筑（garrison） | Mindustry 无对应概念 |
| 乘载运输载具 | payload 系统语义不同 |
| D 部署/收起 | 部署类单位语义不同 |
| G 警戒（独立指令） | Mindustry 默认待机即"自动还击、不追击"，等价能力已内置，无需独立键 |
| 右键拖拽平移镜头 | 已定：镜头跟随完全保留 Mindustry 原版机制，模组不做任何镜头改动；平移走 WASD/鼠标侧键/小地图 |
| 屏幕边缘滚动 | 属镜头行为改动，同上不实现（源码已核实桌面端原版也无此功能） |
| 空格跳到事件位置 | 原版无事件跳转设施，成本/收益不匹配 |

---

## 8. 与 RA2 的差异一览

- **键位迁移**：S 停止 → **H**（S 被 WASD 占用）；G 警戒 → 取消（见 §7），**G 让给攻击移动**；H 跳回基地 → **Home**。
- **镜头**：完全保留 Mindustry 原版机制（跟随玩家单位），RA2 的右键拖镜头、屏幕边缘滚动均不实现（见 §7）。
- **增强**（RA2 没有，模组新增）：Z 左键可视化路径点、Ctrl 框选剔除、三击全图同类、Home 回核心、攻击移动指令（RA2 无 A-move，语义源自 Generals 的融合，见 PLAN §3）。
- **依赖 P0 验证的项**：新键位动态注册（失败则引导改绑现有关键位）、右键 `touchDown` 拦截完整性（右键单击清空需完整屏蔽原版右键下令，是鼠标语义翻转的最大风险）。
- **建造/采矿/蓝图/建筑管理**：不改造，非指挥模式下行为与原版完全一致。
